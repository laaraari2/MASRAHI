'use client'; import {useState} from "react"; import Link from "next/link"; import {FileText, Printer, CheckCircle2} from "lucide-react"; export default function Final(){const [ready,setReady]=useState(false);return <main className="main" style={{marginRight:0}}><header className="top"><div><div className="eyebrow">🎭 MASRAHI</div><h1>العرض النهائي</h1><span className="muted">تجهيز النسخة النهائية للعرض والطباعة.</span></div><Link className="btn secondary" href="/">الرئيسية</Link></header><div className="projects"><article className="card"><FileText size={28}/><h3>نسخة المخرج</h3><p className="muted">النص + الشخصيات + التوزيع + الحركة + الديكور + الصوت والإضاءة + ملاحظات الإخراج.</p><button className="btn primary" onClick={()=>setReady(true)}><Printer size={15}/>&nbsp;تحضير للطباعة</button></article><article className="card"><FileText size={28}/><h3>نسخة التلميذ</h3><p className="muted">الشخصية + المشاهد + الحوار + الحركات الأساسية فقط.</p><button className="btn secondary"><Printer size={15}/>&nbsp;طباعة النسخة</button></article></div>{ready&&<div className="card" style={{marginTop:18}}><CheckCircle2 size={18}/> النسخة جاهزة للمراجعة والطباعة.</div>}</main>}